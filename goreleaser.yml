# Build customization
project_name: terraform-provider-genymotion

builds:
  - binary: terraform-provider-genymotion_v0.0.1
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64

archive:
  files:
  - none*